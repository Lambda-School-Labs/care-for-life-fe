import { StyleSheet } from "react-native";

export default StyleSheet.create({
  textInput: {
    borderWidth: 1,
    width: 200,
    marginBottom: 10,
    textAlign: "left",
  },
  textTitle: {
    textAlign: "center",
  },
});
